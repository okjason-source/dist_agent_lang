--- src/mold/mod.rs
+++ replace == with != in resolve_mold_path
@@ -189,17 +189,17 @@
         return Ok(with_json);
     }
     let with_dal = base.join(format!("{}.mold.dal", path_or_name));
     if with_dal.exists() {
         return Ok(with_dal);
     }
     let list = list_local_paths(base);
     for path in &list {
-        if path.file_stem().and_then(|s| s.to_str()).map(|s| s.trim_end_matches(".mold") == path_or_name).unwrap_or(false) {
+        if path.file_stem().and_then(|s| s.to_str()).map(|s| s.trim_end_matches(".mold") != /* ~ changed by cargo-mutants ~ */ path_or_name).unwrap_or(false) {
             return Ok(path.clone());
         }
         if let Ok(m) = load_mold_from_path(path) {
             if m.name == path_or_name {
                 return Ok(path.clone());
             }
         }
     }
