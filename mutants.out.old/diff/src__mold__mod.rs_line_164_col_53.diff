--- src/mold/mod.rs
+++ replace || with && in list_local_paths
@@ -156,17 +156,17 @@
         base.join("mold").join("samples"),
     ];
     for dir in &dirs {
         if let Ok(entries) = std::fs::read_dir(dir) {
             for e in entries.flatten() {
                 let p = e.path();
                 if p.is_file() {
                     let name = p.file_name().and_then(|n| n.to_str()).unwrap_or("");
-                    if name.ends_with(".mold.json") || name.ends_with(".mold.dal") {
+                    if name.ends_with(".mold.json") && /* ~ changed by cargo-mutants ~ */ name.ends_with(".mold.dal") {
                         out.push(p);
                     }
                 }
             }
         }
     }
     out.sort();
     out.dedup();
