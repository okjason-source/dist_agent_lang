# Build artifacts (rebuilt inside image)
target/
**/target/

# Git and IDE
.git/
.gitignore
.cursor/
.vscode/
*.swp
*.swo
*~

# Dependencies (Rust fetches in-container; Node not needed at runtime)
node_modules/

# Local dev and test outputs
logs/
mutants.out/
*.log
.coverage
htmlcov/
.pytest_cache/
__pycache__/

# Release artifacts (not needed to build image)
dist_agent_lang-*/
*.tar.gz
*.zip

# Docs that aren't needed in minimal runtime (optional: remove to include full docs)
docs/  # Uncomment to exclude docs from image

# Tests (reduce context size). `benches/` must stay: Cargo.toml declares [[bench]] targets and
# `cargo build` fails to parse the manifest if bench sources are missing from the build context.
tests/
*.test.rs
*.bench.rs

# CI and local tooling
.github/
.circleci/
.travis.yml
Makefile
Dockerfile*
docker-compose*
.dockerignore

# Misc
*.md
!README.md
.env
.env.*
*.pem
*.key
