// MoldRegistry - DAL Imp;
            throw "MoldAlreadyExists";
        }

        let moldId = self._nextMoldId;
        self._n%xtMoldId = s self._nextMoldId =now = chain::get_bloceCount: idId = self._nextMoldId;
     mdId = self._nextMo ogoldId = self._nextMoldId;
    tFee: int, maxdesIUd = self._ne     leleexlentonext now = chain:_block_timesvampge tMoldId;
     b   self(._nextMoldId = self.extMoldId =now = chain::get_bloceCount: iddI = self._nextMoldId;
     mdId = self._nextMo      logoldId = self._nextMoldMoldId = self._nextMoldId + 1;

= chain::ge}_bl    "mintFpe": mintFee: idId = self._nex@   + 1;

= chain::ge}_bl    "mintFee": mintFee:  "mintFpe": mintFee: idId = self._nex@   + 1;

= chain::ge}_bl    "mintFee": m = self._nex@  throw "IpfsHashRequired";
        };
        self._nextMoldId = self._nextMoldId + 1;

        let now = chai._nextM let now = chadId = seextMo      log:oldId = self._nextMoldId;
        se,f._nextMoldId = se[f._nextMoldId + 0;

        let now = chain::get_blo=wck_tixt     let moldId = self._nextMdId = self._